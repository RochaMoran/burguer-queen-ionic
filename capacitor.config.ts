import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'burquer_queen',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
